server:
  port: 8080

spring:
  application:
    name: ride-hailing-simulator

  # H2 Database Configuration
  datasource:
    url: jdbc:h2:mem:ridehailing;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  # H2 Console (for development)
  h2:
    console:
      enabled: true
      path: /h2-console

# 调度配置
dispatch:
  enabled: true
  interval-ms: 120000  # 2分钟 = 120000毫秒
  speed-kmh: 30        # 车辆行驶速度 30km/h

# 订单派单配置
order-dispatch:
  enabled: true
  interval-ms: 120000      # 2分钟 = 120000毫秒
  pickup-speed-kmh: 30     # 接乘速度 30km/h
  delivery-speed-kmh: 40   # 履约速度 40km/h

logging:
  level:
    root: INFO
    org.example: DEBUG
    org.hibernate.SQL: DEBUG
